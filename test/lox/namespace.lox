namespace clox.test;
using clox.std.collection;
using clox.std.util.Date;
using clox.std.util.UUID as ID;
using test.lox.Fraction;

println(collection);
println(collection.Array);
println(Date);
println(ID);
println(Fraction);